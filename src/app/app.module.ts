import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common'; // Módulo comum
import { AppComponent } from './app.component'; // Importando o AppComponent

@NgModule({
    imports: [CommonModule],  // Apenas importação de módulos necessários
})
export class AppModule { }
